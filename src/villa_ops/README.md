# villa_ops (planned)

Purpose: internal operations app for Villa Thaifa.

v1 goals:

- read canonical operational data
- expose workflow dashboards
- add limited high-confidence edits only after schema freeze

Current status: placeholder (implementation not started).
