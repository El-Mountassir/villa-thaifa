# Unprocessed Intake

This folder tracks items not yet reconciled into canonical workflows.

Use `manifest.csv` as the single queue for triage.

State values:

- `inbox`
- `working`
- `planned`
- `canonical`
- `archived`
- `blocked`
